annoté:
@Controller
    @GetMapping
        lister(@RequestMapping)

        ModelAndView

-- Apprendre création annotation avant sprint1
    Annotation applicable sur:
    -class
    -method
    -attribut
    (mila package ny annotation)
        -anarana
        -Anarana variable apetraka ao amle annotation
        -variable apetraka ao