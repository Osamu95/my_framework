sprint2
    - exception qui affiche les url allowed et leur method si url invalid
    - si valid affiche la class, la method

sprint3
    - Ajouter methode dans UrlMapping
    - clé de list_map nouvel class: Url_method(url, methode)

sprint3-Bis
    - Si url valide: executer la fonction via reflect(juste metre sysout dans la fonction)